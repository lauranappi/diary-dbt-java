# Copia questo file in .env e metti l'indirizzo vero del backend.
# In sviluppo locale puoi lasciare quello di default (localhost:8080),
# basta che il backend sia avviato con "mvn spring-boot:run".
VITE_API_URL=http://localhost:8080
